DATA SEGMENT
SOURCE DB 'CHEN MING LIANG 16340023!', '$'
COUNT EQU $-SOURCE
DATA ENDS
EXTRA SEGMENT
DEST DB COUNT DUP (?)
EXTRA ENDS
CODE SEGMENT
    ASSUME CS:CODE, DS:DATA, ES:EXTRA
BEGIN:
    MOV AX,DATA
    MOV DS,AX
    MOV BX,EXTRA
    MOV ES,BX
    LEA SI,SOURCE; MOV SI,OFFSET SOURCE
    LEA DI,DEST;   MOV DI,OFFSET DEST
    MOV CX,COUNT
REP MOVS DEST,SOURCE; REP MOVSB
    LEA DX,DEST
    MOV AH, 09H
    INT 21H
    MOV AH, 4CH
    INT 21H
CODE ENDS
    END BEGIN
